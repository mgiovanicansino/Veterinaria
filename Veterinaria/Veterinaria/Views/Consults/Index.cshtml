@model IEnumerable<Veterinaria.Models.Consult>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ConsultDay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripction)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConsultType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ConsultDay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripction)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConsultType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
